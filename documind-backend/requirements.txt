# FastAPI and Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.6

# API Documentation
python-jose[cryptography]==3.3.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
cryptography==41.0.7  # Also used for OAuth token encryption

# CORS and Middleware
starlette==0.37.2

# Rate Limiting
slowapi==0.1.9

# Logging
python-json-logger==2.0.7
structlog==23.2.0

# Background Tasks
celery==5.3.4
redis==5.0.1

# Database (MongoDB Atlas)
motor==3.3.2  # Async MongoDB driver
beanie==1.23.0  # ODM for MongoDB (async)
pymongo==4.6.1  # MongoDB driver (required by motor)

# HTTP Client
httpx==0.25.2
aiohttp==3.9.1

# Utilities
python-dotenv==1.0.0
pytz==2023.3

# Email Service
aiosmtplib==3.0.1
jinja2==3.1.2  # For email templates

# Two-Factor Authentication
pyotp==2.9.0
qrcode[pil]==7.4.2  # For QR code generation

# SSO (OAuth2/OIDC)
authlib==1.3.0

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0
openpyxl==3.1.2
python-pptx==0.6.23
easyocr==1.7.0  # Primary OCR engine (pip-installable, cloud-ready)
pytesseract==0.3.10  # Optional fallback OCR engine (requires system installation)
Pillow==10.1.0
chardet==5.2.0
pandas==2.1.3
unstructured==0.11.6
langdetect==1.0.9  # Language detection for text preprocessing
# Note: Custom RecursiveCharacterTextSplitter implementation in chunking_service.py (no external dependency needed)

# Embedding Services (install only the providers you need)
# OpenAI embeddings (default, requires API key)
openai==1.3.0
# Sentence Transformers (FREE, local, recommended for development)
# sentence-transformers  # Uncomment to use free local embeddings
# Cohere embeddings (optional, requires API key)
# cohere==4.37
# Google Gemini embeddings (optional, requires API key)
# google-generativeai==0.3.0  # Old package, deprecated

# LLM Services (install only the providers you need)
# OpenAI LLM (default, requires API key) - already included above
# Google Gemini LLM (optional, requires API key)
google-genai==1.55.0  # New Google GenAI SDK for Gemini models
# Anthropic Claude LLM (optional, requires API key)
# anthropic==0.18.1

# Retrieval Engine - Re-ranking (optional)
# Cohere Rerank (optional, requires API key)
# cohere==4.37  # Same as Cohere embeddings above
# Cross-encoder Rerank (optional, free, local)
# sentence-transformers  # Same as Sentence Transformers above

# Vector Stores (install only the provider you need)
# ChromaDB (default, recommended for local development)
chromadb==0.4.18
# Pinecone (optional, for cloud deployment)
# pinecone-client==3.0.0
# Qdrant (optional, for self-hosted deployment)
# qdrant-client==1.7.0

# Storage (S3-compatible storage - MinIO, AWS S3, Cloudflare R2)
minio==7.2.0

# Testing (Development)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2

